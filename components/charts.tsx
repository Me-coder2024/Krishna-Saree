'use client';
import {ResponsiveContainer,LineChart,Line,XAxis,YAxis,Tooltip,CartesianGrid,BarChart,Bar} from 'recharts';
import {Product} from '@/lib/catalog';
export default function Charts({events,products}:{events:{product_id:string;event_type:string;created_at:string}[];products:Product[]}){const days=Array.from({length:7},(_,i)=>{const d=new Date(Date.now()-(6-i)*86400000);const date=d.toISOString().slice(0,10);return {day:d.toLocaleDateString('en-IN',{weekday:'short'}),views:events.filter(e=>e.event_type==='view'&&e.created_at.startsWith(date)).length};});const top=products.map(p=>({name:p.name.split(' ').slice(0,2).join(' '),views:events.filter(e=>e.product_id===p.id&&e.event_type==='view').length})).sort((a,b)=>b.views-a.views).slice(0,5);return <div className="chart-grid"><div className="admin-card"><h3>Product views · last 7 days</h3><ResponsiveContainer width="100%" height={250}><LineChart data={days}><CartesianGrid stroke="#eee3d5" vertical={false}/><XAxis dataKey="day" tick={{fontSize:11}}/><YAxis allowDecimals={false} tick={{fontSize:11}}/><Tooltip/><Line type="monotone" dataKey="views" stroke="#6b1526" strokeWidth={2}/></LineChart></ResponsiveContainer></div><div className="admin-card"><h3>Most-viewed pieces</h3><ResponsiveContainer width="100%" height={250}><BarChart data={top}><XAxis dataKey="name" tick={{fontSize:9}}/><YAxis allowDecimals={false} tick={{fontSize:11}}/><Tooltip/><Bar dataKey="views" fill="#c9a24b"/></BarChart></ResponsiveContainer></div></div>;}
